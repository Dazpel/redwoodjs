/**
 * Intercepts and defers any requests on the page
 * until the Service Worker instance is ready.
 */
export declare function deferNetworkRequests(workerReady: Promise<ServiceWorkerRegistration | null>): void;
